% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_era5land.R
\name{extract_era5land}
\alias{extract_era5land}
\title{Extract ERA5-Land hourly data for a bounding box}
\usage{
extract_era5land(
  bbox,
  years,
  output_dir,
  variables = c("surface_solar_radiation_downwards", "2m_dewpoint_temperature",
    "2m_temperature", "total_precipitation", "10m_u_component_of_wind",
    "10m_v_component_of_wind"),
  months = 1:12,
  label = "era5land",
  dataset = "reanalysis-era5-land",
  cds_key = Sys.getenv("CDS_API_KEY"),
  time_out = 7200
)
}
\arguments{
\item{bbox}{Numeric vector of length 4 in CDS order \code{c(N, W, S, E)}
(e.g. the return value of \code{\link[=get_bounding_box]{get_bounding_box()}}).}

\item{years}{Integer vector of years to download.}

\item{output_dir}{Directory to stage NetCDFs under. Created if needed.}

\item{variables}{Character vector of ERA5-Land variable short names.}

\item{months}{Integer months in \code{1:12} to download.}

\item{label}{File-name prefix used for each NetCDF.}

\item{dataset}{CDS \code{dataset_short_name}.}

\item{cds_key}{Optional CDS API key; defaults to \code{Sys.getenv("CDS_API_KEY")}.}

\item{time_out}{Per-request timeout in seconds passed to \code{\link[ecmwfr:wf_request]{ecmwfr::wf_request()}}.}
}
\value{
Character vector of NetCDF paths attempted (invisibly).
}
\description{
Downloads hourly ERA5-Land reanalysis NetCDFs from the Copernicus Climate
Data Store (CDS), one file per (year, month), into
\verb{<output_dir>/<year>/<label>_<year><MM>.nc}. A \code{CDS_API_KEY} environment
variable must be set, or \code{cds_key} passed explicitly.
}
\details{
The function is resume-safe: existing readable NetCDFs are skipped, and
truncated files are re-downloaded. Failures for a single month are logged
and the loop continues.
}
\examples{
\dontrun{
bbox <- get_bounding_box("/vsizip/path/to/area.zip/area.shp")
extract_era5land(
  bbox       = bbox,
  years      = 2020:2021,
  output_dir = "data/climate/era5land/input/nc"
)
}
}
