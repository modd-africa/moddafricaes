% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_era5land.R
\name{transform_era5land}
\alias{transform_era5land}
\title{Transform ERA5-Land hourly NetCDFs into per-year Parquet}
\usage{
transform_era5land(
  input_dir,
  output_dir,
  dem_tif,
  years,
  nc_template = "era5land_\%d\%02d.nc",
  out_template = "era5land_hourly_\%d_\%s.parquet",
  version = "v0.1.0",
  compression = "zstd",
  min_year_rows = 1e+06,
  metadata = NULL
)
}
\arguments{
\item{input_dir}{Directory holding \verb{<year>/<nc_template>} NetCDFs
(typically produced by \code{\link[=extract_era5land]{extract_era5land()}}).}

\item{output_dir}{Directory for year-level Parquet output.}

\item{dem_tif}{Path to a DEM raster covering the AOI. Resampled
onto the ERA5-Land grid once and reused for every month.}

\item{years}{Integer vector of years to process.}

\item{nc_template}{\code{sprintf} template for the input NetCDF basename;
receives \verb{(year, month)}.}

\item{out_template}{\code{sprintf} template for the year-level output
basename; receives \verb{(year, version)}.}

\item{version}{Version tag embedded in the output filename and
the Parquet footer metadata.}

\item{compression}{Parquet compression codec.}

\item{min_year_rows}{Lower bound for a "complete" year file. Year files
with fewer rows are deleted and regenerated. Default \code{1e6} suits a
~165-cell AOI for a full year; widen for larger AOIs.}

\item{metadata}{Named character vector of metadata embedded in the
Parquet footer. Defaults to a generic ERA5-Land dictionary.}
}
\value{
Character vector of year-level Parquet paths (invisibly).
}
\description{
Reads ERA5-Land hourly NetCDFs (one per (year, month)), de-accumulates
\code{tp} and \code{ssrd}, derives wind speed, relative humidity, VPD and Celsius
temperature, joins a DEM-derived elevation column onto the ERA5 0.1 deg
grid, and writes one Parquet file per year. Intermediate per-day Parquets
are staged under \verb{<output_dir>/_daily/<year>/} and removed after the year
file passes a readability check.
}
\details{
The pipeline is resume-safe: unreadable Parquets are deleted and
re-generated, partial year files (below \code{min_year_rows}) are discarded
before regeneration, and per-day failures are logged and skipped.

Hour-0 of each day is forced to 0 for the de-accumulated \code{tp} and \code{ssrd}
columns. This is exact only where 00 UTC falls during night (e.g. central
Africa); for AOIs where 00 UTC is daylight, real downward shortwave flux
would be silently zeroed at the day boundary.
}
\examples{
\dontrun{
transform_era5land(
  input_dir   = "data/climate/era5land/input/nc",
  output_dir  = "data/climate/era5land/output/parquet",
  dem_tif     = "RDC_Elevation_Complete.tif",
  years       = 2014:2017,
  nc_template = "era5land_kinshasa_hourly_\%d\%02d.nc",
  out_template = "era5land_hourly_kinshasa_\%d_\%s.parquet"
)
}
}
